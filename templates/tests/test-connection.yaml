apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "apono-agent.fullname" . }}-test-connection"
  labels:
    {{- include "apono-agent.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "apono-agent.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
